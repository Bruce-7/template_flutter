// 普通用户最大支持录入资产数量，超出弹窗引导
const ordinaryAssetMaxCount = 50;

// 备注图片 非会员每一个资产最多一张，超出弹窗引导
const ordinaryRemarksImageMaxCount = 1;

// 移除背景 累计享受最大次数，超出弹窗引导
const ordinaryBackgroundRemoverMaxCount = 50;

// 附加费用 累计享受最大次数，超出弹窗引导
const ordinaryAdditionalCostsMaxCount = 50;

// 到期通知 累计享受最大次数，超出弹窗引导
const ordinaryExpiryAlertMaxCount = 50;

// 资产标签 非会员每一个资产最多添加一个，超出弹窗引导
const ordinaryMaxTagCount = 1;

// 资产标签 会员每一个资产最多添加数量
const premiumMaxTagCount = 20;

// 备注图片 会员每一个资产最多数量
const premiumRemarksImageMaxCount = 9;

// ========== 礼记相关限制 ==========

// 礼记人员 非会员最多添加人员数量，超出弹窗引导
const ordinaryGiftRecordMaxCount = 10;

// 礼记往来记录 非会员每个人员最多添加记录数量，超出弹窗引导
const ordinaryGiftRecordItemMaxCount = 1;

// 礼记备注图片 非会员每条往来记录最多添加图片数量，超出弹窗引导
const ordinaryGiftRecordItemImageMaxCount = 1;

// 礼记备注图片 会员每条往来记录最多添加图片数量
const premiumGiftRecordItemImageMaxCount = 9;
